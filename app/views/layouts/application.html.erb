<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "After School Activites" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous"> <%# FROM ASSIGNMENT 5%>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="usercontrol">
      <% unless controller_name == 'sessions' %>
        <% if current_user == nil %>
          <div class="loginout"><%= button_to "Log In", "/users/sign_in" %></div>
        <% else %>
          <div class="user"><%= "Current User: #{current_user.email}" %></div>
          <div class="loginout"><%= button_to "Log Out", "/users/sign_out", method: :delete %> </div>
        <% end %>
      <% end %>
    </div>
    <% if flash[:notice] %>
      <div class="alert alert-primary">
        <%= "#{flash[:notice]}" %> 
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="alert alert-warning">
        <%= "#{flash[:alert]}" %>
      </div>
    <% end %>
    
    
    
    <%# FROM ASSIGNMENT 5 END %>
    <br>
    <div class="innerboddy"> 
      <%= yield %>
    </div>
    <br>

    <footer class="footer">
      <%= render "application/footer", locals: {current_user: current_user}%>
    </footer>
  </body>
</html>
