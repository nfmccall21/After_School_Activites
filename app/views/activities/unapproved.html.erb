<h1>Unapproved Activities</h1><br>
<%= link_to "Back to Activities", activities_path, class: "btn btn-secondary backtoactivities" %> <br><br>

<% if @unapproved_activities.any? %>
    <div id="unapproved-activities">
        <% @unapproved_activities.each do |activity| %>
            <div>
                <h3><%= activity.title %></h3>
                <p><%= activity.description %></p>
                <p><strong>Chaperone:</strong> <%= activity.chaperone %></p>
                <p><strong>Day:</strong> <%= activity.day %></p>
                <p><strong>Time:</strong> <%= activity.time_start.strftime('%l:%M %p') %> - <%= activity.time_end.strftime('%l:%M %p') %></p>
                <p><strong>Status:</strong> Unapproved</p>
            
                <!-- Linking approve/deny -->
                <%= form_with url: accept_activity_path(activity), method: :patch, local: true do %>
                    <%= submit_tag "Approve" %>
                <% end %>
                <%= form_with url: decline_activity_path(activity), method: :patch, local: true do %>
                    <%= submit_tag "Deny" %>
                <% end %>

            </div>
            <br>
        <% end %>
    </div>
<% else %>
    <p>No unapproved activities found.</p>
<% end %>
